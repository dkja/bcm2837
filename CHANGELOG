

v0.1
    * IO_mem    - map physical I/O-memory to virtual memory and access it.
    * GPIO      - simple GPIO support based on IO_mem: 
                        set pin,
                        clear pin,
                        get value from pin